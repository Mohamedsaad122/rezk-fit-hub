/**
 * Application LocalStorage key identifiers registry.
 */
export const STORAGE_KEYS = {
    ACCESS_TOKEN: 'authToken',
    REFRESH_TOKEN: 'refreshToken',
    THEME: 'theme',
    LANGUAGE: 'language',
    USER: 'user',
    SETTINGS: 'settings',
};

export default STORAGE_KEYS;
